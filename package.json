{
  "name": "babel-plugin-no-console-logs",
  "version": "1.0.0",
  "description": "A babel plugin for removing console logs",
  "main": "index.js",
  "homepage": "https://github.com/JayaKrishnaNamburu/babel-plugin-no-console-logs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "babel plugins --out-dir dist",
    "build": "npm run clean:build && babel src --out-dir build",
    "clean:build": "rimraf build/*"
  },
  "keywords": [
    "babel",
    "babel-plugin",
    "no-console-logs",
    "remove logs",
    "console logs"
  ],
  "author": "Jaya Krishna Namburu",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.2.3",
    "@babel/types": "^7.2.2"
  },
  "dependencies": {
    "@babel/template": "^7.2.2"
  }
}
